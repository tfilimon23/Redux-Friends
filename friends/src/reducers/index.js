const initialState = {
    friends: [],
    error: null,
    loggingIn: false,
    fetchingFriends: false
};

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
        return state;
    }
};

export default reducer;

